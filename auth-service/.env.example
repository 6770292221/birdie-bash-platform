# Auth Service Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
# MongoDB connection string for user database
# For Docker setup: use the connection string below
# For local MongoDB: mongodb://localhost:27017/birdie_auth
USER_DB_URI=mongodb://admin:password123@localhost:27017/birdie_auth?authSource=admin

# JWT Configuration
# IMPORTANT: Use a strong, unique secret in production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# CORS Configuration (optional)
# Add allowed origins if needed for specific frontend domains
# CORS_ORIGIN=http://localhost:3000,http://localhost:8080