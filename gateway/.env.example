# Gateway Service Configuration

# Server Configuration
PORT=8080
NODE_ENV=development

# JWT Configuration
# IMPORTANT: Must match the JWT_SECRET in auth-service
# Use a strong, unique secret in production
JWT_SECRET=your-secret-key

# Microservice URLs
# URLs for the backend services that the gateway will proxy to
AUTH_SERVICE_URL=http://localhost:3001
EVENT_SERVICE_URL=http://localhost:3003
SETTLEMENT_SERVICE_URL=http://localhost:3006

# CORS Configuration (optional)
# Add allowed origins if needed for specific frontend domains
# CORS_ORIGIN=http://localhost:3000

# API Documentation Configuration (optional)
# Customize Swagger documentation settings
# SWAGGER_TITLE=Birdie Bash Platform API
# SWAGGER_VERSION=1.0.0
# SWAGGER_DESCRIPTION=API Gateway for Birdie Bash Platform
